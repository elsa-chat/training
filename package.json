{
  "name": "semoss-training-site-tests",
  "version": "1.0.0",
  "description": "UI validation tests for ${CONFIG.productName} training site",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
